---
title: Skaya | Introduction
description: Discover Skaya, the advanced React Component Library that supercharges your apps by exposing browser Navigator APIs as beautiful, themed components and intuitive hooks.
---

# Skaya SDK & SkayaJS UI Library

<div
  style={{
    display: "flex",
    alignItems: "center",
    borderRadius: "4px",
    height: "20px",
    border: "2px solid red",
    padding: "1rem",
  }}
>
  <a
    href="https://www.npmjs.com/package/skaya"
    target="_blank"
    style={{
      display: "flex",
      alignItems: "center",
      gap: "0.5rem",
      color: "#cb3837",
      textDecoration: "none",
      fontWeight: "bold",
    }}
  >
    <img
      src="/logo/npm-logo-red.png"
      alt="npm logo"
      style={{ height: "12px" }}
    />
    <span>| Skaya v1.0.0</span>
  </a>
</div>

::: details Organization
In Skaya, the organization serves as the top-level entity where all participants of your group are added. You can be a part of an organization soon.

Skaya is a **full-stack automation SDK** that helps developers scaffold, manage, and integrate frontend, backend, and blockchain projects with a single CLI.

SkayaJS is a **React component library** that complements Skaya by providing reusable UI components and hooks for browser APIs.

:::

## 📦 Skaya (`skaya`)

**Skaya** is a Node.js-based CLI SDK focused on **project automation and developer workflows**.

<div className="flex justify-center my-6">
  <img src="/skayainfo.png" alt="Skaya Info" className="rounded-xl shadow-md w-2/3" />
</div>

### ✨ Features

- Scaffold new projects (frontend, backend, blockchain) with templates.
- Create, update, and manage components with AI assistance.
- Start and install project environments (frontend, backend, blockchain).
- Extend with middleware, routes, and integrations.
- Designed for Web2 + Web3 full-stack automation.

### 🚀 Example Usage

```bash
 npm install -g skaya
```

```bash
 skaya init frontend
```

```bash
 skaya create component
```

## 🎨 SkayaJS (`skayajs`)

**SkayaJS** is a React-based UI library that provides **ready-to-use components and hooks** for browser APIs and frontend utilities.  
It is distributed separately to keep the main SDK (`skaya`) lightweight.

### ✨ Features

- Reusable UI components like `LocationButton`.
- Utility hooks such as `useGeolocation`.
- Designed to integrate seamlessly into any React app.
- Small, tree-shakable, and framework-agnostic (React 16.8+).

### 🚀 Example Usage

```bash
 npm install skayajs 
```

```tsx
import { LocationButton, useGeolocation } from "skayajs";

function App() {
  const { location, isLoading, fetchLocation } = useGeolocation();

  const fetchLocationAndSendTobackend = async () => {
    await fetchLocation();
    await fetch("/api/location", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(location),
    });
  };

  return (
    <LocationButton onClick={fetchLocationAndSendTobackend} isLoading={isLoading}>
      Get Location
    </LocationButton>
  );
}

```

## 🔗 How They Work Together

- **Skaya (CLI SDK)** → automates project creation and frontend and web3 workflows. Creates Automated Stories/ Test files.
- **SkayaJS (React UI library)** → provides optional UI components and hooks.

You can use **only Skaya** for CLI/automation, or add **SkayaJS** for UI features.

## 📚 Learn More

- [SkayaJS UI Library](/guide/React-sdk/introduction.html)
- [Skaya SDK Library](/guide/Cli-sdk/Introduction.html)
