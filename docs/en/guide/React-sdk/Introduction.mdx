---
title: Skaya | Introduction
description: Discover Skaya, the advanced React Component Library that supercharges your apps by exposing browser Navigator APIs as beautiful, themed components and intuitive hooks.
---

# Skaya: The Navigator API React Component Library
<div
  style={{
    display: "flex",
    alignItems: "center",
    borderRadius: "4px",
    height: "20px",
    border: "2px solid red",
    padding: "1rem",
  }}
>
  <a
    href="https://www.npmjs.com/package/skaya"
    target="_blank"
    style={{
      display: "flex",
      alignItems: "center",
      gap: "0.5rem",
      color: "#cb3837",
      textDecoration: "none",
      fontWeight: "bold"
    }}
  >
    <img
      src="/logo/npm-logo-red.png"
      alt="npm logo"
      style={{ height: "12px" }}
    />
    <span>| Skaya v1.0.0</span>
  </a>
</div>

::: details Organization
In Skaya, the organization serves as the top-level entity where all participants of your group are added. You can be a part of an organization soon.
:::

## SkayaJS 🚀

SkayaJS is an advanced **React Component Library** that transforms powerful **Browser Navigator APIs** (like `navigator.geolocation`, `navigator.bluetooth`, and more) into beautiful, themed React components and intuitive hooks.

Stop writing raw API calls! Instead of interacting directly with **`navigator.geolocation`** or **`navigator.bluetooth`**, you can simply drop in a **`<Geolocation />`** or **`<BluetoothConnector />`** component or use its corresponding hook.

## ✨ Features

- ⚡ **Navigator-based Components** – Access APIs like **Geolocation**, Bluetooth, and Clipboard through ready-to-use, styled components.  
- 🧩 **Companion Hooks** – Every component has a dedicated hook (e.g., **`useGeolocation`**, **`useClipboard`**) allowing you to manage state and logic without rendering the UI component.  
- 🔌 **Tree-shakable** – Keep your bundle small. Import only the components and hooks your application needs.  
- ⚡ **Universal** – Seamlessly integrates with modern React frameworks like **Vite**, **Next.js**, and standard **Create React App**.  

## 📚 Installation

```bash
npm install skayajs
# or
yarn add skayajs
```