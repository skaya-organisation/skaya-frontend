---
title: SkayaJS Quickstart Guide
description: Learn how to install SkayaJS, initialize components, and use browser APIs with theming support.
keywords:
  - Skaya
  - quickstart
  - geolocation
  - react
  - installation
  - hooks
---

# Quickstart | Get Started in 5 Minutes

<div
  style={{
    display: "flex",
    alignItems: "center",
    borderRadius: "4px",
    height: "20px",
    border: "2px solid red",
    padding: "1rem",
  }}
>
  <a
    href="https://www.npmjs.com/package/skaya"
    target="_blank"
    style={{
      display: "flex",
      alignItems: "center",
      gap: "0.5rem",
      color: "#cb3837",
      textDecoration: "none",
      fontWeight: "bold",
    }}
  >
    <img
      src="/logo/npm-logo-red.png"
      alt="npm logo"
      style={{ height: "12px" }}
    />
    <span>| Skaya v1.0.0</span>
  </a>
</div>

## ✅ Prerequisites

- **Node.js**: v18+
- **npm** or **yarn** 

## Installation

```bash
npm install skayajs
# or
yarn add skayajs
```

## Basic Usage: Geolocation Example

SkayaJS provides hooks and controlled components for common browser APIs. Here’s how to use the useGeolocation hook with the new LocationButton component.

```tsx
import React from "react";
import { useGeolocation, LocationButton } from "skayajs";

export default function App() {
  const { location, isLoading, fetchLocation } = useGeolocation();

  const isLocationAvailable = location.latitude !== null && location.longitude !== null;

  return (
    <div style={{ padding: "1rem", fontFamily: "sans-serif" }}>
      <h2>My GPS Tracker</h2>

      <LocationButton
        onClick={fetchLocation}
        isLoading={isLoading}
        style={{ padding: "0.5rem 1rem", fontSize: "1rem" }}
      >
        Get My Location
      </LocationButton>

      {/* Display position if available */}
      {isLocationAvailable && (
        <p>
          Latitude: {location.latitude!.toFixed(5)}, Longitude:{" "}
          {location.longitude!.toFixed(5)}
        </p>
      )}
    </div>
  );
}
```

## 🎨 Theming

- All components adapt automatically (background, text, etc.).

- You can pass custom CSS classes via className on each component.
