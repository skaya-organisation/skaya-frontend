---
title: SkayaJS Quickstart Guide 
description: Learn how to install SkayaJS, initialize components, and use browser APIs with theming support.
keywords:
  - Skaya
  - quickstart
  - navigator
  - react
  - installation
---

# Quickstart | Get Started in 5 Minutes

<div
  style={{
    display: "flex",
    alignItems: "center",
    borderRadius: "4px",
    height: "20px",
    border: "2px solid red",
    padding: "1rem",
  }}
>
  <a
    href="https://www.npmjs.com/package/skaya"
    target="_blank"
    style={{
      display: "flex",
      alignItems: "center",
      gap: "0.5rem",
      color: "#cb3837",
      textDecoration: "none",
      fontWeight: "bold"
    }}
  >
    <img
      src="/logo/npm-logo-red.png"
      alt="npm logo"
      style={{ height: "12px" }}
    />
    <span>| Skaya v1.0.0</span>
  </a>
</div>

## ✅ Prerequisites

- **Node.js**: v18+  
- **npm** or **yarn**  

## Installation

```bash
npm install skayajs
# or
yarn add skayajs
```
## 🚀 Basic Usage
SkayaJS provides hooks and controlled components for browser APIs. Here’s how to use the GeoLocation hook with the GetLocationButton:

```tsx
import React from "react";
import { useGeolocation, GetLocationButton } from "skayajs";

export default function App() {
  const { location, loading, getPosition } = useGeolocation();

  return (
    <div style={{ padding: "1rem", fontFamily: "sans-serif" }}>
      <h2>My GPS Tracker</h2>

      <GetLocationButton
        label="Get My Location"
        loadingLabel="Locating..."
        onGetPosition={getPosition}
        isLoading={loading}
        style={{ padding: "0.5rem 1rem", fontSize: "1rem" }}
      />

      <div style={{ marginTop: "1rem" }}>
        {location.error && <p style={{ color: "red" }}>{location.error}</p>}
        {location.latitude && location.longitude && (
          <p>
            Latitude: {location.latitude.toFixed(5)}, Longitude: {location.longitude.toFixed(5)}
          </p>
        )}
        {!location.error && !location.latitude && !loading && <p>Click the button to fetch location.</p>}
      </div>
    </div>
  );
}
```

## 🎨 Theming

- All components adapt automatically (background, text, etc.).

- You can pass custom CSS classes via className on each component.

